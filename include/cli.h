#ifndef CLI_H
#define CLI_H

void cli_start();      // inicia o CLI (loop de comandos)
void cli_help();       // mostra comandos disponíveis
void cli_logo();       // imprime o logo GrayDB
void cli_clear();      // limpa a tela (se suportado)

#endif
